name: bora_ai
description: Bora AI - Africa's Intelligence Engine
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # UI & Icons
  lucide_icons_flutter: ^3.0.0
  
  # HTTP & API
  http: ^1.3.0
  
  # Storage
  shared_preferences: ^2.5.5
  
  # File Upload
  image_picker: ^1.1.2
  file_picker: ^8.0.0
  
  # Voice Input - Updated version
  speech_to_text: ^7.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true